# -----------------------------------------------
# !!! THIS FILE WAS GENERATED BY TYPE-GRAPHQL !!!
# !!!   DO NOT MODIFY THIS FILE BY YOURSELF   !!!
# -----------------------------------------------

# The javascript `Date` as string. Type represents date and time as the ISO Date string.
scalar DateTime

# Item from the marketplace
type Item {
  id: Int!
  name: String!
  price: Float!
}

type Mutation {
  createZombie(zombie: ZombieInput!): Zombie!
  updateZombie(zombie: ZombieInput!, id: Int!): Zombie!
  deleteZombie(id: Int!): Zombie!

  # Adds selected item to zombie's inventory. The item must be unique for given
  # zombie, the inventory can have up to 5 items total.
  addZombieItem(itemId: Int!, zombieId: Int!): Zombie!

  # Removes selected item from zombie's inventory.
  removeZombieItem(itemId: Int!, zombieId: Int!): Zombie!
}

type Query {
  zombie(id: Int!): Zombie
  zombies: [Zombie!]!
  item(id: Int!): Item
  items: [Item!]!
}

type TotalPrice {
  pln: Float!
  usd: Float!
  eur: Float!
}

type Zombie {
  id: Int!
  name: String!
  createdAt: DateTime!
  items: [Item!]!

  # Total value of zombie's inventory in PLN, USD and EUR
  totalPrice: TotalPrice!
}

input ZombieInput {
  name: String!
}
